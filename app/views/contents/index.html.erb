<%= render "shared/header" %>

<div class='main'>

<%# ログインしていなければ表示する %>
<%# <h1 class="top-name">sign in</h1>

<div class="sign-box">
  <div class="account-btn">log in</div>
  <div class="account-btn">new account</div>
</div> %>
<%# //ログインしていなければ表示する %>

<%# ログインしていれば表示する %>
<div class="main-wraper">
  <%= link_to "new create", new_content_path, class: "new-edit" %>

  <% @content.each do |content| %>
    <ul class="main-list">
      <li class="list-day">
        <%= l content.created_at,  format: :short %> 
        <%= link_to "#{content.title}", content_path(content.id), method: :get, class:"list-day-a"%>
      </li>
    </ul>
  <% end %>
</div>
<%# //ログインしていれば表示する %>
<%= render "shared/footer" %>